apiVersion: apps/v1
kind: Deployment
spec:
    metadata:
    spec:
      containers:
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources: {}
